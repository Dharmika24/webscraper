# webscraper
This project is a simple Python script that automatically collects the latest news headlines from a public news website. It uses requests to fetch the webpage and BeautifulSoup to extract all &lt;h2> elements, which typically contain headline text. All extracted headlines are saved into a file named headlines.txt. 
